# Handoff: GENERAL degree-`d` — algebraic + abstract-geometric obstruction SKELETON complete & axiom-clean

**Date**: 2026-06-14 11:45 UTC · **Branch**: `st06` · **HEAD**: `2461395` (+ this doc)

## 🎯 What we're doing
Formalizing Stoll [St06] in Lean 4. All #482/St05/St06 headlines are DONE & axiom-clean; the cubic
AND quartic unconditional a.e.-`W` self-referential impossibilities are DONE & axiom-clean (repo is
fully sorry-free + custom-axiom-free). **This lap pushed the next frontier — the *uniform* general
degree-`d` (`α = 2^{1/d}`) impossibility — and built its entire algebraic + abstract-geometric
obstruction skeleton.** These were exactly the three per-degree obstacles the handoff flagged as not
generalizing from the hand-rolled cubic/quartic proofs. Build 🟢 8293; everything
`[propext, Classical.choice, Quot.sound]`-only.

## ✅ Landed this lap (4 commits, all axiom-clean)
- **`General/RpowLinIndep.lean` — `rpow_lin_indep_int`**: for **every** `d ≥ 1`,
  `{1, 2^{1/d}, …, 2^{(d-1)/d}}` are ℤ-linearly independent. THE hard brick. Route: **Eisenstein at
  the prime 2** ⇒ `Xᵈ−2` irreducible over ℤ (`Polynomial.IsEisensteinAt.irreducible`) ⇒ over ℚ
  (Gauss for GCD domains, `IsPrimitive.irreducible_iff_irreducible_map_fraction_map`) ⇒ it IS
  `minpoly ℚ (2^{1/d})` (`minpoly.eq_of_irreducible_of_monic`), degree `d` ⇒ no nonzero degree-`<d`
  rational poly vanishes at the root (`minpoly.degree_le_of_ne_zero` + `degree_sum_fin_lt`). Mathlib's
  Kummer `X_pow_sub_C_irreducible_of_prime` is **prime-exponent only** (misses `d=4,6,8,9,…`);
  Eisenstein covers all `d` uniformly. *(Beats the per-degree cubic-elimination / quartic 2-adic
  descent with one proof — those bespoke files are kept, not refactored.)*
- **`General/RpowWindow.lean`**: `rrt_lt_four_thirds` (`2^{1/d}<4/3` for `d≥3`, via `2<(4/3)ᵈ`),
  **`rrt_window_gt_two`** (partial-defect range width `α+…+α^{d-1}>2` for `d≥3`; key collapse: `αᵈ=2`
  ⇒ geometric sum `= (2-α)/(α-1)`, `>2 ⟺ α<4/3`), and **`window_not_cover`** (a width-2 window
  `(C-2,C]` can't cover a width-`>2` interval `[0,W)` — the abstract escape). `d=2` gives `√2<2`: the
  obstruction begins exactly at `d=3` (consistent with the solvable Graham–Pollak `√2` case).
- **`General/GeneralDefect.lean`** — the degree-agnostic defect engine (replaces the hand-rolled
  `linear_combination`s): `affine_rec_closed` (closed form of `v(k+1)=αv(k)+b(k)`), defs
  `dStepV`/`dStepF`/`dStepC`/`dStepDefect`, **`dStep_defect_identity`** (`v_d=2u+C−D`,
  `C=∑α^{d-k}cₖ`, `D=∑α^{d-1-k}fₖ`), `dStepPartial` + `dStep_last_arg`/`dStep_last_fract_forced`
  (last floor error forced `f_e={C−g}`), `dStep_digit_eq_floor` (`digit=⌊C−g⌋`),
  **`dStep_partial_mem_window`** (a base-2 digit ⇒ `g∈(C-2,C]`), plus `dStepPartial_eq_sum`
  (`g=∑_{k<e}α^{e-k}fₖ`) and range bounds `dStepPartial_{nonneg,lt_window}` (`g∈[0,S_d)`).
- **`General/RpowWindow.lean` (abstract crux, also added)** — `sum_pos_coeff_realize` (a
  positive-coefficient sum surjects onto `[0,S)`) and **`exists_partial_defect_outside_window`** (for
  `d≥3`, any `C`, a reachable fract-config has `g ∉ (C-2,C]`). **So no fixed `C` makes every reachable
  partial defect a base-2 digit — the algebraic core is now CLOSED.**
- **`General/GeneralDefectCheck.lean` (faithfulness)** — `cubicV3_eq_dStepV`/`quarticV4_eq_dStepV`
  (the cubic/quartic maps ARE the `d=3,4` instances of `dStepV`) and `{cubic,quartic}V*_sub_eq_via_general`
  (the general `dStep_defect_identity` reproduces, term-for-term, the independently-verified cubic AND
  quartic defect identities). A genuine cross-check that the generalization is faithful to both proven cases.
- **`General/GeneralOrbit.lean` (analytic bridge STARTED)** — `dStepF_orbit`: each floor error `fₖ` at
  `u=⌊W2ⁿ⌋` expressed via the doubling-orbit coordinates `{αⁱW2ⁿ}` and previous errors, ONE uniform
  lemma (via `affine_rec_closed`) replacing the cubic's two hand-rolled `cubic_f{1,2}_orbit`. This is
  the entry point to the remaining `Tᵈ` assembly.
- All files wired into `Erdos482General.lean` (so the pre-commit `lake build` gate checks them); 13 commits
  this lap, every one green (8295 jobs at HEAD).

## 🧠 Mental model (carry forward)
The general-`d` obstruction, in one breath: the `d`-step floor map's extracted digit is `⌊C−g⌋` where
`g` is the partial defect (`= ∑_{k<d-1} α^{d-1-k} fₖ`, the floor errors *except the last*, which is
forced). A base-2 digit forces `g ∈ (C-2,C]` (width 2). But `g` ranges over `[0, α+…+α^{d-1})`, width
`>2` for `d≥3` (`rrt_window_gt_two`). So a dense orbit of `g` must leave the window
(`window_not_cover`) ⇒ some step has a non-base-2 digit. The density is the only analytic input, and
it reduces (via `rpow_lin_indep_int` ⇒ `ξ=∑nᵢαⁱ≠0`) to `Tᵈ` equidistribution on the
**already-degree-agnostic** `MultidimWeyl`/`EquidistDense`/`DELEngine` engine.

## 🎬 Next actions (the analytic `Tᵈ` assembly — mirror the cubic/quartic finish)
1. **Orbit-coordinate form** — `dStepF_orbit` DONE (floor errors via orbit coords). NEXT: define a
   general `dGpd` (mirror `cubicGpd`) — a recursive `orbitF α c r : ℕ → ℝ` building the `fₖ` from the
   coordinate vector `r` (`r i = {αⁱW2ⁿ}`), so that `dStepPartial(⌊W2ⁿ⌋) = ∑ α^{e-k} (orbitF … k)` via
   `dStepPartial_eq_sum` + `dStepF_orbit`. *Gotcha:* `orbitF k` references `orbitF j` for `j ∈ range k`
   (recursion under `Finset.sum`) — the equation compiler won't auto-terminate; define via
   `Nat.strongRecOn` (pass a `g : (j:ℕ) → j < k → ℝ`) rather than pattern-matching. Then prove
   `ContinuousAt` (mirror `continuousAt_cubicGpd`, by induction on `k` with `continuousAt_fract`).
2. **General `Tᵈ` equidistribution/density** a.e.-`W`: feed `rpow_lin_indep_int` into the Weyl reduction
   (`weyl_criterion_torus` + `ae_comp_mul_left`), exactly as `ae_W_cubic_torus_orbit_dense` but for
   `Fin d`. Engine is degree-free — should be a near-mechanical generalization.
3. **Geometry crux — DONE abstractly** (`exists_partial_defect_outside_window`): for any `C` a reachable
   fract-config gives `g ∉ (C-2,C]`. What remains is purely connecting it to the *torus orbit* via #1
   (i.e. the orbit's fract-coords are dense ⇒ they hit such a config). No standalone Aristotle target
   left here. (Aristotle all IDLE.)
4. **Headline**: `ae_no_dStep_schedule_reads_base_two` for all `d ≥ 3`, uniform over schedules — chain
   #1+#2 (dense `Tᵈ` orbit) against `dStep_partial_mem_window` + `exists_partial_defect_outside_window`.

## ⚠️ Gotchas (this lap)
- `import Mathlib.Algebra.BigOperators.Basic` does NOT exist → use
  `Mathlib.Algebra.BigOperators.Group.Finset.Basic`.
- Gauss `Monic.irreducible_iff_irreducible_map_fraction_map` needs `[IsIntegrallyClosed ℤ]` (instance
  not in scope by default) — use the **`IsPrimitive.…` variant** (GCD-domain Gauss) instead.
- `Finset.sum_Ico_consecutive` + `← Finset.range_eq_Ico` rewrites **both** `Ico 0 _` occurrences → use
  `geom_sum_Ico` (closed form `(xⁿ-xᵐ)/(x-1)`) directly instead of splitting by hand.
- `Real.rpow_lt_rpow` (base-monotone, `0≤x→x<y→0<z→x^z<y^z`) is namespaced `Real`; `geom_sum_eq`/
  `geom_sum_Ico` are `_root_` (not Finset-namespaced).
- DO NOT push (Trevor merges). Pre-commit runs full `lake build`; commit every green build.

## 📁 Key files (this lap)
- `src/Erdos482/General/RpowLinIndep.lean` — `rpow_lin_indep_int` (Eisenstein, all `d`).
- `src/Erdos482/General/RpowWindow.lean` — `rrt_window_gt_two`, `rrt_lt_four_thirds`, `window_not_cover`.
- `src/Erdos482/General/GeneralDefect.lean` — `dStep_defect_identity` + g-collapse + window.
- `PENDING_WORK.md` (top ★★★★★) — the general-`d` skeleton + the 4-step analytic-assembly frontier.
- `STATUS.md` — refreshed overview + axiom ledger (cubic/quartic + general-`d` rows added).

---
**→ Next session: the general-`d` algebraic skeleton is DONE. Pick up at Next action #1 (orbit-coordinate
form), or architect the geometry crux (#3) for Aristotle. The analytic engine is already degree-agnostic,
so this is largely a `Fin d` re-run of the cubic finish, not new math.**
